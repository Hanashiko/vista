version: '3.8'

services:
  react-app:
    build: .
    container_name: my-react-app
    ports:
      - "3003:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
