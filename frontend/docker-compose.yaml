version: '3.8'

services:
  react-app:
    build: .
    container_name: vista-web
    ports:
      - "3003:80"
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
