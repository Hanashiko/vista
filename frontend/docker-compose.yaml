version: '3.8'

services:
  react-app:
    build: .
    container_name: my-react-app
    ports:
      - "3003:80"  # Прокидуємо порт 3001 на хості -> 80 в контейнері
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
